import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  logo: {
    height: 150,
    width: 150,
    borderColor: 'black'
  },
  container: {
    minHeight: '100%',
    minWidth: '100%',
    flex: 1
  },
  textbig: {
    fontSize: 16
  }
})

export default styles
